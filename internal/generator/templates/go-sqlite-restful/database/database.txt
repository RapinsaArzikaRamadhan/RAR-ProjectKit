package database

import (
	"database/sql"
	_ "modernc.org/sqlite"
)

func DB() (*sql.DB, error) {
	db, err := sql.Open("sqlite", "database.db")
	if err != nil {
		return nil, err
	}
	return db, nil

}